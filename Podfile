# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'MobiTalksTDDiOS' do
  use_frameworks!

  pod 'lottie-ios', '2.5.0'

  target 'MobiTalksTDDiOSTests' do
    inherit! :search_paths
    pod 'Nimble', '7.3.0'
  	pod 'Quick', '1.3.1'

    end
end
